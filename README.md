# How to run the code:
With a machine that can run python, do:
```
python eswe_ec.py
```
This will run the code, along with the test currently implemented. This can be modified to to test other edge cases or perform other uses.

# Write Up
This assignment could be further improved upon by organizing the instructions to include the necessary functions, along with the expected operations of those functions. 
Also adding a potential input or test case file for students to run, then comparing output to expected to create a sort of testing suite. The assignment would need to specify
the expected program language, but this could be decided by whatever is currently most popular in industry use (most likely python). The assignment could also ask students to
compare the program's function to how an actual database operates, noting similarities and differences.